<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.newsupplytech.jxsmh.mapper.front.NewsDetailMapper">
    <select id="queryDetail"  resultType="News">
        select id,title,createtime,details from news where id=#{id}
    </select>
</mapper>